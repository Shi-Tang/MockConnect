/** Gemini Live native audio session (Simulation). Keep in sync with Simulation.tsx `live.connect` model. */
export const SIMULATION_LIVE_MODEL = 'gemini-2.5-flash-native-audio-preview-12-2025';

/** Post-session Session Review scoring (generateContent). Keep in sync with Simulation.tsx feedback step. */
export const SESSION_REVIEW_MODEL = 'gemini-3-flash-preview';
